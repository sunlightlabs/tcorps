<%= errors @user %>

Current Avatar:<br/>
<%= image_tag @user.avatar.url(:normal) %>

<% form_for @user, :html => {:multipart => true} do |form| %>
  Upload a new avatar: 
  <%= form.file_field :avatar %>
  <%= submit_tag 'Update' %>
<% end %>